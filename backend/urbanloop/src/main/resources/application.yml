spring:
  datasource:
    url: jdbc:h2:mem:urbanloop;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: "cambia-esta-clave-por-una-bien-larga-y-segura-para-firma"
    expiration: 86400000 # 1 día en milisegundos
